{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "clang++ build",
            "command": "clang++",
            "type": "shell",
            "args": [
                "${workspaceFolder}/src/test.cpp",
                "-o",
                "${workspaceFolder}/src/test.out",
                "-g", // for debug symbols
                "-march=haswell", // for AVX2 features
                "--std=c++14"
            ]
        }
    ]
}
